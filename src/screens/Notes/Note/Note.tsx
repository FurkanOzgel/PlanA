import React from 'react';
import {
    SafeAreaView,
    Text
  } from 'react-native';

function Note(): React.JSX.Element {
    return(
        <SafeAreaView>
            <Text>Note</Text>
        </SafeAreaView>
    );
};

export default Note;