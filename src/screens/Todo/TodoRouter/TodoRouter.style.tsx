import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {},
    todoListCard: {},
    defaultContainer: {
        borderBottomColor: "white",
        borderBottomWidth: 1,
    },
});